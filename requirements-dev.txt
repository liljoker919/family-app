# Dev/CI-only dependencies for the Playwright E2E suite (e2e/) — kept out of
# requirements.txt so a production install never pulls in a browser engine.
# Install alongside requirements.txt: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

playwright>=1.47.0
pytest>=8.3.0
pytest-django>=4.9.0
pytest-playwright>=0.5.2
pytest-html>=4.1.1
